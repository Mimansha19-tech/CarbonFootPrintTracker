/*
	Placeholder Supabase client wrapper. Replace with real Supabase init when ready:
	import { createClient } from '@supabase/supabase-js'
	export const supabase = createClient(SUPABASE_URL, SUPABASE_KEY)
*/
console.log('supabaseClient placeholder')